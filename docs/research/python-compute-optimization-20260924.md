# Python 回测与评价提速：2026-09-24

## 范围

美股8765、A股日线8767；不修改8768、数据源、评价门槛、任务预算或交易逻辑。
本轮没有替换Rust权威内核，没有启用研究任务或调用LLM。

## 实现

1. 每个事件日限定的持仓估值缓存。全持仓估值保持原有positions插入顺序和逐项浮点加法；不使用增量总和、补偿求和或不同顺序的数组归约。
2. 现金不缓存；成交、公司行动和缺行情减记均使受影响持仓估值及合计失效。step正常返回或异常退出后清空缓存。step之外的直接调用继续使用参考实现。
3. 事件执行行不再携带forward return和label日期列；完整原始帧仍用于因果IC、诊断和输入哈希。
4. 评价表达式在执行前，仅裁掉目标层结束后的年份。保留所有此前历史行和完整截面；不提前截掉历史预热、不按未来Top-N名单过滤。

## 对照方法

`scripts/benchmark_python_optimizations.py`：同进程、同面板，先预热。每个配置旧/新交替运行两次，取中位耗时。参考路径通过环境开关选择。

- 因子21量价动量表达式，Top500，5日调仓，头部20%。
- 普通场景：2021-01-01至2026-09-23；美股纯多、美股多空、A股纯多。
- 高级风控场景：2023-01-01至2026-09-23；逆波动定仓、GTC续挂、平方根冲击、固定止损止盈、ATR退出、保本、最长持有、组合回撤/单日亏损及冷却、期末强平。
- 全量成交、事件、每日账本、持仓、绩效、年度/滚动表现、IC、2000条固定种子蒙特卡洛及相关性结果参与相同JSON哈希检查。
- 计时/后端诊断、带时间戳的输入来源描述不参加输出哈希；输入来源和完整配置另存报告。
- 不创建生产回测记录，不改变因子生命周期；仅产生审计快照和本次基准报告。

## 最终复测（中位秒数）

| 场景 | 参考 | 优化 | 加速 |
|---|---:|---:|---:|
| 美股纯多 | 6.068 | 4.481 | 1.35x |
| 美股多空 | 20.987 | 9.267 | 2.26x |
| A股纯多 | 7.832 | 6.053 | 1.29x |

训练评价单次暖启动：美股纯多0.975→0.829秒，美股多空1.107→0.880秒，A股1.405→1.098秒。分数、方向与门槛结果一致；单次计时不代表所有表达式的固定增益。

原始最终报告：`var/reports/python-optimization-parity-20260924-final/report.json`。
首次探索报告保留在`var/reports/python-optimization-parity-20260924/report.json`，部署依据以最终报告为准。

## 测试与边界

完整测试集：446 passed、3 subtests passed；唯一警告为既有Starlette/httpx弃用提醒。
六组真实回测均通过精确输出哈希与账本完整性检查。三组完整向量评价（含训练、HOLDOUT、Vault、2020至最新评级）排除计时后精确相等；同时包含双向训练原始收益证据。

标准测试命令为`.venv/bin/python -m pytest -q tests`，明确限定正式测试目录，避免A股部署目录的历史备份副本被pytest递归收集。

8765和8767已重启，进程中两个优化开关均为1，HTTP首页及引擎状态接口正常；研究任务仍为暂停状态。A股部署前文件备份：`/Users/jiangjingzhe/Portfolios/AShareFactorFactory/var/python-performance-backup.8EDLwP`。未覆盖本地环境文件、数据库或历史回测账本。

新增60种接受配置的合成逐日精确对照，包含跳空、拆股、缺行情、买卖限制、各种退出、定仓和账户规则；另测异常清理、现金独立变化和嵌套时序/截面表达式的历史预热。

事件账本要求精确相等。Polars分位收益均值的并行分组归约，在旧版重复执行间已有约1.7e-18的浮点抖动；这项使用绝对1e-15容差，日级训练路径仍精确比较。没有放宽正式入库阈值。

本次不是普遍的两倍加速保证：低成交量策略受益较少。没有实现跨任务因子缓存、袖套多进程并行或Rust全面替换；这些需要各自的内存、失效与对齐设计。

## 回退

默认启用；可在各服务本地`.service.env`中单独设置并重启：

```sh
FF_PYTHON_EVENT_OPTIMIZATIONS=0
FF_EVALUATION_WINDOW_OPTIMIZATIONS=0
```

不要关闭账本、输入冻结或因果标签检查。回测响应`execution.python_optimizations`包含是否开启及实现协议。
